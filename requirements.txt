# ============================================================
# SAM Assistive Vision — Python Dependencies
# ============================================================
# Install with: pip install -r requirements.txt
# ============================================================

# --- Web Framework & Server ---
fastapi==0.115.5
uvicorn[standard]==0.32.0
websockets==13.1
python-multipart==0.0.12

# --- Configuration ---
pydantic-settings==2.6.1

# --- Image Processing ---
pillow>=11.1.0
numpy>=2.1.0

# --- HTTP Client (Ollama API calls) ---
httpx==0.27.2

# --- TTS (Stage 6 -- active) ---
edge-tts>=7.0.0

# ============================================================
# Future dependencies (uncomment when entering each phase):
# ============================================================

# Stage 1: STT — faster-whisper
# faster-whisper>=1.0.0     # CTranslate2 backend, 4-10x faster than openai/whisper
# soundfile>=0.12.0         # WAV audio loading
# sounddevice>=0.4.0        # Microphone recording (mic_test.py demo)

# Stage 3: SAM — FastSAM via ultralytics
# torch>=2.2.0
# torchvision
# ultralytics>=8.0.0        # FastSAM-s.pt and FastSAM-x.pt

# Stage 4: BLIP — image captioning via HuggingFace
# transformers>=4.40.0
# torch>=2.2.0              # shared with SAM
# accelerate>=0.27.0        # GPU memory optimization
# ============================================================
