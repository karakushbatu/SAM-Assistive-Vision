# ============================================================
# SAM Assistive Vision — Python Dependencies
# ============================================================
# Install with: pip install -r requirements.txt
# ============================================================

# --- Web Framework & Server ---
fastapi==0.115.5
uvicorn[standard]==0.32.0
websockets==13.1
python-multipart==0.0.12

# --- Configuration ---
pydantic-settings==2.6.1

# --- Image Processing (needed from Phase 2 onward, installed early) ---
# Pinned versions removed — allow pip to find a wheel compatible with the
# current Python version (3.14+). Minimum versions are enforced below.
pillow>=11.1.0
numpy>=2.1.0

# --- HTTP Client (used in Phase 4 for Ollama API calls) ---
httpx==0.27.2

# --- TTS (Phase 4A — active now) ---
edge-tts>=7.0.0

# ============================================================
# Future dependencies (uncomment when entering each phase):
# ============================================================
# Phase 2 — SAM
# torch>=2.2.0
# torchvision
# ultralytics        # FastSAM
# segment-anything   # MobileSAM
#
# Phase 3 — Classifier
# open-clip-torch    # CLIP
# transformers       # HuggingFace (alternative)
# ============================================================
